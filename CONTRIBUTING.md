# Contributing

Use TypeScript strict mode and Rust formatting. Test scans only against fixtures you own. Do not add features that upload file contents or broaden file deletion scope. Run `npm run check`, `npm run build`, `cargo fmt --check`, `cargo test`, and `cargo check` before a pull request.

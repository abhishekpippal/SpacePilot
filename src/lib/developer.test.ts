import {describe,expect,it} from "vitest";import {developerFamily,filterDeveloperFindings} from "./developer";import type {DeveloperStorageFinding}from"../types";
const finding=(category:string,size_bytes:number,confidence:"HIGH"|"MEDIUM"|"LOW"="HIGH"):DeveloperStorageFinding=>({id:category+size_bytes,category,subtype:"test",path:"C:\\test",name:"test",size_bytes,item_count:1,modified:null,confidence,risk:"REVIEW_REQUIRED",explanation:"why",review_action:"review",source:"test",reviewable:true});
describe("developer storage presentation logic",()=>{it("maps ecosystem filters",()=>{expect(developerFamily("NODE_DEPENDENCIES")).toBe("Node.js");expect(developerFamily("AI_MODEL")).toBe("AI/ML");expect(developerFamily("VM_STORAGE")).toBe("VM/WSL")});it("filters without changing input",()=>{const input=[finding("RUST_BUILD",2),finding("NODE_DEPENDENCIES",4)];expect(filterDeveloperFindings(input,"Rust","size")).toHaveLength(1);expect(input[0].size_bytes).toBe(2)});it("sorts by size, category, and confidence",()=>{const input=[finding("RUST_BUILD",2,"LOW"),finding("NODE_DEPENDENCIES",4,"HIGH")];expect(filterDeveloperFindings(input,"All","size")[0].size_bytes).toBe(4);expect(filterDeveloperFindings(input,"All","category")[0].category).toBe("NODE_DEPENDENCIES");expect(filterDeveloperFindings(input,"All","confidence")[0].confidence).toBe("HIGH")})});
